%h1 Bienvenido a la aplicación
%p Por favor, inicia sesión o regístrate.

- if user_signed_in?
  %p Hola, #{current_user.email}
  = form_with(url: destroy_user_session_path, method: :delete, style: 'display: inline;') do
    = submit_tag 'Cerrar sesión', class: 'button'
- else
  = link_to 'Iniciar sesión', new_user_session_path
  |
  = link_to 'Registrarse', new_user_registration_path
